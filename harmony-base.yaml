name: harmony-base
channels:
  - conda-forge
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pip
  - uvicorn
  - fastapi
  - pydantic
  - pydantic-settings
  - sqlalchemy
  - alembic
  - python-multipart
  - flask
  - git
  - numpy
  - scipy
  # GPU Toolchain from conda (install in step B)
  - pip:
      # Hugging Face & extras
      - huggingface-hub
      - transformers
      - accelerate
      # App-specific deps
      - passlib[argon2]
      - python-jose[cryptography]
      - cryptography
      # (Optional — skip for now if you don't need it)
      # - triton
      # - kernels
      # - git+https://github.com/triton-lang/triton.git@main#subdirectory=python/triton_kernels
